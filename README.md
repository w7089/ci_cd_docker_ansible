# Basic commands

- build base image

`docker build -t warrior7089/todobackend-base todobackend-base`

- build dev image

```
cd todobackend
make build
```

- run unit and integration tests

`make test`
